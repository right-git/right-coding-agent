"""right-code: an async coding-assistant agent with desktop computer use.

Packages: `llm` (model client, agents, and all LLM tools — web parser,
computer use, the run_tools sandbox), `ui` (terminal chat), `config`,
`utils`. Entry point: `python -m src.main`.
"""
